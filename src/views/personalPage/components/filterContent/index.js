import React from "react";

const FilterContentComponent = (props) => {
  return (
    <div className="filter_content_container">
      {props.component}
    </div>
  );
};

export default FilterContentComponent;
